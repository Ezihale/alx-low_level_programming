#include "main.h"

/**
 * print= prints 10 times the alphabet-Entry point
 *
 * Return: 0
 */
void prints 10 times the alphabet(void)

{
	char c = 'a';
	int i = 0

	while (i <= 9)
	{
		for (c = 'a'; c <= 'z'; c++)
		{
			_putchar(c);
		}
		_putchar ('\n');
		i++;
	}
}
